# Publicação no Netlify (modo demonstração: sem login e com escolhas salvas no navegador).
# Para login por e-mail e escolhas na nuvem, publique no Cloudflare Pages (ver README).
[build]
  base = "cpa-perguntas"
  command = "npm run build"
  publish = "dist"
